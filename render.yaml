services:
  - type: web
    name: sentinelai
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app/app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0